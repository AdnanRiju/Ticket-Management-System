@model CompanyManagement.Models.ProductType
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@{
    Layout = null;
}

<div class="modal-dialog modal-dialog-centered" role="document">
    <div class="modal-content">
        <div class="modal-header bg-info text-white">
            <span><b>@ViewBag.TitleName</b></span>
            <span><b>Add  new Type</b></span>
            <button type="button" class="close trigger-reload text-white" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
        </div>
        <div class="modal-body">
            <form asp-controller="Product" asp-action="AddProductType"
                  data-ajax-begin="onBegin" data-ajax-complete="onComplete"
                  data-ajax-failure="onFailed" data-ajax-success="onSuccessForTicket"
                  data-ajax="true" data-ajax-method="POST" autocomplete="off">
                @Html.AntiForgeryToken()
                <div class="card-body">
                    <div class="form-group row">
                        <label for="Product type" class="col-md-4 col-form-label">Product type Name</label>
                        <div class="col-md-8">
                            <input type="hidden" asp-for="Id" />
                            <input class="form-control" asp-for="Product_Type" required="required" placeholder="ProductType" />
                            <span class="text-danger field-validation-valid" asp-validation-for="Product_Type" data-valmsg-replace="true"></span>
                        </div>
                    </div>
                </div>
                <div class="madal-footer float-right">
                    <input type="submit" class="btn btn-sm btn-success" value="Save" />
                </div>
            </form>
        </div>
    </div>
</div>


<script>
    $(document).ready(function () {
        $('.trigger-reload').on('click', () => {
            setTimeout(() => {
                location.reload()
            }, 400);
        });
    });
</script>